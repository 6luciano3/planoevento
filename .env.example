# La base geográfica del plano (Pantalla 09) usa la API pública de Overpass
# sobre datos de OpenStreetMap — es gratuita y no necesita cuenta ni clave.
# OVERPASS_API_URL es opcional: solo hace falta si preferís apuntar a otra
# instancia (por ejemplo una propia, en vez de https://overpass-api.de).
OVERPASS_API_URL=

DATABASE_URL=
